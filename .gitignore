# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/

# Secrets & credentials — never commit these
.env
.env.local
.env.development
.env.production
# keep .env.example (it's a template, not a secret)
*.key
secrets.yaml
secrets.json
*_secret*
*_token*

# HA addon runtime data (lives in /data inside the container, not here)
data/
options.json
flask_secret.key

# Local dev environment data (dev/run-backend.py writes here)
dev/data/
dev/.env
# Self-signed TLS certs generated by proxy.py --https
dev/certs/

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~

# Logs
*.log
logs/
